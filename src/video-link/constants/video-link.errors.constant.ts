/**
 * not found error for video link
 */
export const VIDEO_LINK_NOT_FOUND_ERROR = 'Video link not found';

/**
 * Conflict error when trying to update titleId while episodeId is set
 */
export const CONFLICT_EPISODE_EXIST_ERROR =
  'Cannot update titleId while episodeId is set';

/**
 * Conflict error when trying to update episodeId while titleId is set
 */
export const CONFLICT_TITLE_EXIST_ERROR =
  'Cannot update episodeId while titleId is set';
